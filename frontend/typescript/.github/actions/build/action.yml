name: build
description: Docker image for this module (GHCR push or compose build via DEPLOY_MODE).
inputs:
  module_dir:
    required: true
runs:
  using: composite
  steps:
    - uses: ./.github/actions/docker-build
      with:
        module_dir: ${{ inputs.module_dir }}
