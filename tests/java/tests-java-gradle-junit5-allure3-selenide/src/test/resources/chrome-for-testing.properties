# Single source for the pinned Chrome for Testing build (browser + matching chromedriver).
# Consumers: helpers.LocalChromePin and scripts/install-chrome-for-testing.sh.
# CI pins the same build in .github/workflows/ci.yml for browser-actions/setup-chrome.
# Per-run override: -DchromeForTestingVersion=... (Java), CHROME_FOR_TESTING_VERSION=... (installer).
version=148.0.7778.178
