package tests;

import annotations.Language;
import annotations.Module;
import io.qameta.allure.Owner;

/**
 * Shared Allure module/language labels for this tests slot (local and CI → TestOps).
 */
@Owner("stanislav")
@Module("tests-java-gradle-junit5-allure3-selenide")
@Language("java")
public abstract class AllureMeta {
}
