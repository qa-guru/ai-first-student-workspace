/**
 * Fixture for `panels.testsTable` — vendored from kit test/fixtures/tests-table-panel.json
 * (standalone consumer; no monorepo path).
 */
import testsTableFixture from "./tests-table-panel.json" with { type: "json" };

export const TESTS_TABLE_FIXTURE = testsTableFixture;
