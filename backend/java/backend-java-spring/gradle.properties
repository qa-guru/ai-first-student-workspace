# Skip the configuration phase when nothing it depends on changed.
# Locally this alone is enough. In CI two more pieces are required (see unit-tests in ci.yml):
# a stable GRADLE_ENCRYPTION_KEY via setup-gradle, and an actions/cache entry for this
# directory — either alone is a miss.
org.gradle.configuration-cache=true
