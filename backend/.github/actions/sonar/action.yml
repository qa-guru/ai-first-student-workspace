name: sonar
description: Dispatch sonar to the active module (module_dir).
inputs:
  module_dir:
    required: true
runs:
  using: composite
  steps:
    - id: dir
      uses: ./.github/actions/resolve-module-dir
      with:
        module_dir: ${{ inputs.module_dir }}
    - if: env.BACKEND_LANG == 'java'
      uses: ./backend/java/backend-java-spring/.github/actions/sonar
      with:
        module_dir: ${{ steps.dir.outputs.dir }}
    - if: env.BACKEND_LANG == 'kotlin'
      uses: ./backend/kotlin/backend-kotlin-spring/.github/actions/sonar
      with:
        module_dir: ${{ steps.dir.outputs.dir }}
    - if: env.BACKEND_LANG == 'python'
      uses: ./backend/python/.github/actions/sonar
      with:
        module_dir: ${{ steps.dir.outputs.dir }}
    - if: env.BACKEND_LANG == 'go'
      uses: ./backend/go/.github/actions/sonar
      with:
        module_dir: ${{ steps.dir.outputs.dir }}
    - if: env.BACKEND_LANG == 'javascript'
      uses: ./backend/javascript/.github/actions/sonar
      with:
        module_dir: ${{ steps.dir.outputs.dir }}
    - if: env.BACKEND_LANG == 'typescript'
      uses: ./backend/typescript/.github/actions/sonar
      with:
        module_dir: ${{ steps.dir.outputs.dir }}
    - if: >-
        env.BACKEND_LANG != 'java' &&
        env.BACKEND_LANG != 'kotlin' &&
        env.BACKEND_LANG != 'python' &&
        env.BACKEND_LANG != 'go' &&
        env.BACKEND_LANG != 'javascript' &&
        env.BACKEND_LANG != 'typescript'
      shell: bash
      run: |
        echo "STOP: no sonar action for BACKEND_LANG=${BACKEND_LANG:-unset}" >&2
        exit 1
